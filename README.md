# konuqWeb

Static, read-only viewer for Konuq published hubs.

## Routes
- `/p/<shareId>` loads `published_hubs/<shareId>/payload.json` from Supabase Storage and renders the hub.

## Configuration
The Supabase URL is currently hardcoded in [p.html](file:///Users/cristianocalicchia/Desktop/konuqWeb/p.html).
